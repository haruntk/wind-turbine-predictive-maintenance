# =============================================================
# Wind Turbine Edge Pipeline — Environment Variables
# =============================================================
# Copy this file to .env and fill in your actual values.
# These variables are resolved by config.yaml at runtime.
# =============================================================

# TimescaleDB connection
DB_HOST=192.168.1.100
DB_NAME=edge_db
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
